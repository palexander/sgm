{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'if command -v sgm >/dev/null 2>&1; then exec sgm hook pretool; fi; printf \"%s\\n\" \"SGM hook skipped: install the sgm binary to enable governance hooks.\" >&2; exit 0'"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'if command -v sgm >/dev/null 2>&1; then exec sgm hook posttool; fi; printf \"%s\\n\" \"SGM hook skipped: install the sgm binary to enable governance hooks.\" >&2; exit 0'"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'if command -v sgm >/dev/null 2>&1; then exec sgm hook stop; fi; printf \"%s\\n\" \"SGM hook skipped: install the sgm binary to enable governance hooks.\" >&2; exit 0'"
          }
        ]
      }
    ]
  }
}
